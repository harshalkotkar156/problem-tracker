{
  "name": "problem-tracker",
  "version": "1.0.0",
  "description": "Full-stack MERN Problem Tracker",
  "scripts": {
    
    "dev": "npm run server",
    "server": "cd backend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
